{
  "private": true,
  "name": "@jsjobs/example",
  "version": "0.1.0-wip",
  "license": "MIT",
  "main": "dist",
  "repository": "github:eropple/jsjobs",
  "scripts": {
    "producer": "node ./dist/producer",
    "consumer": "../service/bin/jsjobs.js --config-file cfg/example.config.js",
    "test": "tslint --project . && jest",
    "build": "tsc",
    "watch": "tsc --watch --preserveWatchOutput",
    "clean": "rm -rf dist",
    "precommit": "npm run test",
    "prepush": "npm run test"
  },
  "dependencies": {
    "@jsjobs/client": "0.1.0-wip",
    "@jsjobs/service": "0.1.0-wip",
    "@types/chance": "^1.0.0",
    "bunyan": "^1.8.12",
    "bunyan-prettystream-circularsafe": "^0.3.1",
    "chance": "^1.0.16",
    "luxon": "^1.2.0",
    "sleep-promise": "^6.0.0"
  },
  "devDependencies": {
    "@types/bunyan": "^1.8.4",
    "tslint": "^5.9.1",
    "typescript": "^2.7.2"
  }
}
